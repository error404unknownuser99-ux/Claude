name: Ubuntu XFCE Cyberpunk RDP

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/heavy-data-processing.yml'
      - 'scripts/process_data.py'
  workflow_dispatch:

jobs:
  ubuntu-rdp:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - uses: actions/checkout@v4

      - name: System Optimize
        run: |
          sudo swapoff -a
          echo 0 | sudo tee /proc/sys/vm/swappiness
          sudo systemctl stop snapd 2>/dev/null || true
          sudo systemctl stop unattended-upgrades 2>/dev/null || true
          sudo systemctl stop apt-daily.timer 2>/dev/null || true
          echo "Optimized! RAM: $(free -h | grep Mem | awk '{print $2}')"

      - name: Install XFCE + XRDP + Themes
        run: |
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            xfce4 xfce4-terminal xfce4-goodies \
            xrdp dbus-x11 x11-xserver-utils xorgxrdp \
            fonts-noto fonts-firacode \
            papirus-icon-theme arc-theme \
            wget curl git unzip firefox \
            xfce4-taskmanager neofetch
          echo "Packages installed!"

      - name: Create User + Fix Black Screen
        run: |
          sudo useradd -m -s /bin/bash cyberpunk 2>/dev/null || true
          echo "cyberpunk:Cyber@2025!" | sudo chpasswd
          sudo usermod -aG sudo cyberpunk

          # Directories pehle banao
          sudo mkdir -p /home/cyberpunk/.config/xfce4/xfconf/xfce-perchannel-xml
          sudo mkdir -p /home/cyberpunk/.config/gtk-3.0

          # BLACK SCREEN FIX 1: .xsession proper shebang ke saath
          sudo tee /home/cyberpunk/.xsession > /dev/null << 'EOF'
          #!/bin/bash
          unset DBUS_SESSION_BUS_ADDRESS
          unset XDG_RUNTIME_DIR
          export XDG_SESSION_TYPE=x11
          export XDG_CURRENT_DESKTOP=XFCE
          export XDG_SESSION_DESKTOP=xfce
          exec startxfce4
          EOF
          sudo chmod +x /home/cyberpunk/.xsession

          # BLACK SCREEN FIX 2: .xsessionrc
          sudo tee /home/cyberpunk/.xsessionrc > /dev/null << 'EOF'
          export XDG_SESSION_DESKTOP=xfce
          export XDG_CURRENT_DESKTOP=XFCE
          export XDG_SESSION_TYPE=x11
          EOF

          # Theme configs
          sudo tee /home/cyberpunk/.config/xfce4/xfconf/xfce-perchannel-xml/xfwm4.xml > /dev/null << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <channel name="xfwm4" version="1.0">
            <property name="general" type="empty">
              <property name="theme" type="string" value="Arc-Dark"/>
              <property name="use_compositing" type="bool" value="false"/>
              <property name="animate_mouse" type="bool" value="false"/>
              <property name="box_move" type="bool" value="false"/>
              <property name="box_resize" type="bool" value="false"/>
            </property>
          </channel>
          EOF

          sudo tee /home/cyberpunk/.config/xfce4/xfconf/xfce-perchannel-xml/xsettings.xml > /dev/null << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <channel name="xsettings" version="1.0">
            <property name="Net" type="empty">
              <property name="ThemeName" type="string" value="Arc-Dark"/>
              <property name="IconThemeName" type="string" value="Papirus-Dark"/>
            </property>
            <property name="Gtk" type="empty">
              <property name="FontName" type="string" value="Noto Sans 10"/>
              <property name="EnableAnimations" type="bool" value="false"/>
            </property>
          </channel>
          EOF

          sudo tee /home/cyberpunk/.config/gtk-3.0/settings.ini > /dev/null << 'EOF'
          [Settings]
          gtk-theme-name=Arc-Dark
          gtk-icon-theme-name=Papirus-Dark
          gtk-font-name=Noto Sans 10
          gtk-enable-animations=0
          EOF

          sudo tee /home/cyberpunk/.gtkrc-2.0 > /dev/null << 'EOF'
          gtk-theme-name="Arc-Dark"
          gtk-icon-theme-name="Papirus-Dark"
          gtk-font-name="Noto Sans 10"
          gtk-enable-animations=0
          EOF

          sudo chown -R cyberpunk:cyberpunk /home/cyberpunk/
          echo "User + config done!"

      - name: Configure XRDP - Black Screen Fix
        run: |
          sudo adduser xrdp ssl-cert 2>/dev/null || true

          # BLACK SCREEN FIX 3: startwm.sh
          sudo tee /etc/xrdp/startwm.sh > /dev/null << 'EOF'
          #!/bin/sh
          unset DBUS_SESSION_BUS_ADDRESS
          unset XDG_RUNTIME_DIR
          export XDG_SESSION_TYPE=x11
          export XDG_CURRENT_DESKTOP=XFCE
          export XDG_SESSION_DESKTOP=xfce
          exec startxfce4
          EOF
          sudo chmod +x /etc/xrdp/startwm.sh

          # BLACK SCREEN FIX 4: sesman.ini - Xorg use karo (better than Xvnc!)
          sudo sed -i 's/^param=Xvnc/param=Xorg/' /etc/xrdp/sesman.ini 2>/dev/null || true

          # Performance tune
          sudo sed -i 's/max_bpp=32/max_bpp=24/' /etc/xrdp/xrdp.ini 2>/dev/null || true
          sudo sed -i 's/#tcp_send_buffer_bytes=32768/tcp_send_buffer_bytes=4194304/' /etc/xrdp/xrdp.ini 2>/dev/null || true

          sudo systemctl restart xrdp
          sleep 4
          sudo systemctl status xrdp --no-pager
          echo "XRDP ready!"

      - name: Start bore tunnel with Auto-Reconnect
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p output
          wget -q "https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-x86_64-unknown-linux-musl.tar.gz" -O bore.tar.gz
          tar -xzf bore.tar.gz
          chmod +x bore

          git config user.name "error404unknownuser99-ux"
          git config user.email "error404unknownuser99@users.noreply.github.com"

          attempt=0
          while true; do
            attempt=$((attempt + 1))
            echo "=== Tunnel attempt #$attempt ==="

            ./bore local 3389 --to bore.pub > output/bore_out.txt 2>&1 &
            BORE_PID=$!
            sleep 8

            PORT=$(grep -oP 'listening at bore\.pub:\K\d+' output/bore_out.txt 2>/dev/null)

            if [ -n "$PORT" ]; then
              echo "TUNNEL LIVE! bore.pub:$PORT"

              cat > output/rdp_credentials.txt << CREDS
          USERNAME: cyberpunk
          PASSWORD: Cyber@2025!
          ADDRESS:  bore.pub:$PORT
          ATTEMPT:  $attempt
          CREDS

              git add output/
              git diff --staged --quiet || (git commit -m "Ubuntu RDP LIVE bore.pub:$PORT" && \
                for i in 1 2 3 4 5; do
                  git pull --rebase origin main && git push && break
                  sleep 2
                done)

              wait $BORE_PID
              echo "Bore exited! Reconnecting in 5s..."
              sleep 5
            else
              echo "Port not found, retrying..."
              kill $BORE_PID 2>/dev/null || true
              sleep 5
            fi
          done
